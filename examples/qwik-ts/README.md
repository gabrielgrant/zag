# Zag Qwik Examples

Qwik v2 examples for Zag machines.

```bash
pnpm dev
```

The current example set contains `/menu/basic`, which is enough to run the Qwik Playwright target:

```bash
pnpm e2e-qwik
```
